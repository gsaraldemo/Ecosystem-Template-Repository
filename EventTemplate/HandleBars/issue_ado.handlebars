{ "issues": [
{{#each value}}
  	{
    	"title": "{{fields.[System.Title]}}",
        "assignee": "{{fields.[System.AssignedTo].displayName}}",
        "status": "{{fields.[System.State]}}"
    },
  {{/each}}
  {}
]}
