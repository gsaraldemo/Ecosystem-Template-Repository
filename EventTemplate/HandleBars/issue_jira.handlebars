{ "issues":[
{{#each issues}}
  	{
    	"title": "{{fields.summary}}",
        "assignee": "{{fields.assignee.displayName}}"
        "status": "{{fields.status.statusCategory.name}}"
    },
{{/each}}
    {}
]
}
