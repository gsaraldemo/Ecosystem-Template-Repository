{
"type": "customPayload",
"actual_payload":{
"blocks": [
{{#each message}}
	  {
        "type": "section",
		"fields": [
		{
			"type": "mrkdwn",
			"text": "*Service*\n{{#each message}}{{truncate this.data.service.name}}\n{{\each}}"
		}
    ]
    {{/each}}
   ]
  }
}
